#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define endl "\n"

int main() { 
    int a, b, c, d;
    cin >> a >> b;
    cin >> c >> d;
    cout << (a*d) - (b*c) << endl;
    return 0;
}
