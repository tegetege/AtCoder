#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int N ,first_W, R, W;
    string c;
    cin >> N;
    cin >> c;
    rep(i, N) {
        for(int i = 0; i < N; i++) {
            if (c[i] = 'W') {
                first_W = i;
                break;
            }
        }
        for()
    }


    return 0;
}
