#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    char a;
    cin >> a;

    if (isupper(a)) {
        cout << 'A' << endl;
    } else {
        cout << 'a' << endl;
    }

    return 0;
}
