#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    int a ;
    cin >> a;
    cout << a + pow(a,2) + pow(a,3) << endl;

    return 0;
}
