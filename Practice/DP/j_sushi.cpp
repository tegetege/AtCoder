#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define endl "\n"

int main() { 
    int N ;
    cin >> N;
    vector<int> a(N, 0);
    rep(i, N) {
        cin >> a[i];
    }

    

    // 確認
    // for(auto e: a){
    //     cout << e << " ";
    // }cout << endl;

    return 0;
}
